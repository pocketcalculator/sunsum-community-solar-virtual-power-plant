/**
 * Public interface of the site-owner dashboard prototype.
 *
 * The feature is browser-safe and uses local mock data only. A future
 * integrated dashboard should receive an authorized composed view from S-VIEW
 * rather than importing backend modules into presentation code.
 */
export { SiteOwnerDashboard } from "./components/SiteOwnerDashboard";
