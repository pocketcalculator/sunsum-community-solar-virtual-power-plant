# Copy to `.env.local` and adjust. `.env.local` is gitignored; this file is not.
#
# Nothing here is a real secret. The local database password matches
# docker-compose.yml and protects a throwaway container.

# The local PostgreSQL from docker-compose.yml. Port 55432, not 5432, to avoid
# colliding with an existing installation.
DATABASE_URL=postgresql://sunsum:devpassword@localhost:55432/sunsum

# Which store the backend reads from.
#
#   mock  the in-memory BackendStore in core/store (the default)
#   db    the implemented PostgresBackendStore selected by composition.ts
#
# `npm run db:seed` populates the canonical local schema. With db selected, the
# workflow endpoints use DATABASE_URL and SUNSUM_DB_AUTH below. Database mode
# requires a reachable, migrated database and grants; it does not provision
# Azure or replace fixed demo identities with authenticated participants.
# Separate PG*/SUNSUM_DATABASE_AUTH operator tooling does not configure this store.
SUNSUM_STORE=mock

# How the backend proves who it is to PostgreSQL. Leave unset unless the
# hostname is misleading.
#
#   password  a password from DATABASE_URL (the local container)
#   entra     a Microsoft Entra access token (the Azure flexible server, which
#             is provisioned with password authentication disabled)
#
# Unset means "decide from the host": anything ending in
# .postgres.database.azure.com uses Entra, everything else uses the password.
# Set it explicitly when that inference is wrong — a private endpoint reached
# through an alias, or a developer pointing at the cloud server from a
# workstation with `az login`.
#
# SUNSUM_DB_AUTH=entra

# Which user-assigned managed identity to authenticate as, when the container
# has more than one. A system-assigned identity leaves this unset. Only read on
# the Entra path.
#
# AZURE_CLIENT_ID=

# Which blob client the backend uses for site documents.
#
#   memory   an in-process map (the default) — no Azure account, no credentials
#   azurite  the local emulator from docker-compose.yml (`npm run blob:up`)
#   azure    the real storage account below
#
# Same seam as SUNSUM_STORE: `core/documents/storage.ts` decides the container
# and blob name either way, so switching this changes where the bytes land and
# nothing else. An unrecognised value throws rather than falling back to memory,
# because a typo would otherwise look like a working deployment whose uploads
# vanish on restart.
SUNSUM_BLOB=memory

# Only read when SUNSUM_BLOB=azurite. The default is Azurite's published
# development credential — the same fixed string in every installation, reaching
# nothing but a local emulator — so it is only set here to point somewhere other
# than 127.0.0.1:10000.
# AZURE_STORAGE_CONNECTION_STRING=

# Which viability service screens a submitted site.
#
#   demo  the in-process fixture (the default) — the same illustrative numbers
#         for every site, so the journey is demonstrable with nothing else
#         running
#   http  the preliminary-viability service, at SUNSUM_VIABILITY_URL
#
# Same seam as SUNSUM_STORE and SUNSUM_BLOB. `core` owns the `ViabilityClient`
# interface; `src/backend/viability` speaks HTTP to the service and translates
# between the two vocabularies. An unrecognised value throws rather than falling
# back, because a typo would otherwise look like a working deployment that
# quietly screened every site with the same fixture numbers.
#
# The service is a separate deployment with its own API. On the `http` path a
# submission that cannot reach it returns 503 and persists neither the site nor
# an assessment, rather than recording a screening that never happened.
SUNSUM_VIABILITY=demo

# Only read when SUNSUM_VIABILITY=http. No trailing slash needed; one is
# ignored. The adapter posts to `${SUNSUM_VIABILITY_URL}/assessments`.
#
# SUNSUM_VIABILITY_URL=http://127.0.0.1:8000

# How long to wait for the viability service before giving up, in milliseconds.
# Defaults to 8000. A site submission blocks on this call, so the timeout is
# what stops a slow upstream from becoming a hung form.
#
# SUNSUM_VIABILITY_TIMEOUT_MS=8000

# The dev storage account, used when SUNSUM_BLOB=azure. Shared keys are disabled
# on it, so there is no connection string or account key to set here — access is
# Entra-only.
#
# Locally, `az login` is the credential; deployed, it is the app's managed
# identity. Either principal needs the "Storage Blob Data Contributor" role on
# the account, which is a separate grant from being able to see it.
AZURE_STORAGE_ACCOUNT_NAME=stsunsumsolardevcus

# The key the session cookie is signed with. At least 32 characters.
#
# Sessions carry only a user id and the time they were issued; the role is read
# from the user row on every request, so a role that changes in the database
# takes effect immediately and a stolen cookie cannot claim a role it was not
# given. What the signature buys is that the user id cannot be edited.
#
# Unset in development, the backend generates one per process and says so. That
# means sessions end when the dev server restarts, which is a fair trade for
# needing no setup. In production an unset secret is refused outright rather
# than generated, because a per-instance secret presents as users being signed
# out at random and is miserable to diagnose.
#
# Generate one with:
#   node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"
#
# (Require the module explicitly. The global `crypto` is the Web Crypto API and
# has no `randomBytes`; a bare `crypto` only works in `node -e` because of a
# legacy auto-require shim that does not apply in a real script.)
#
# SUNSUM_SESSION_SECRET=

# Whether POST /api/auth/demo-switch is reachable. Set it to `enabled` to turn
# it on; anything else, including unset, leaves it off.
#
# The demo sign-in hands out one of three *seeded* identities so the three
# roles can be shown without an identity provider. It is not a credential
# check: anyone who can reach the endpoint can become any of the three demo
# users, including the operator.
#
# It is therefore opt-in. A deployment that left this on by default while
# serving real records would be no better protected than one with no sign-in at
# all, so turning it on is a decision a deployment records rather than one it
# inherits. The demo deployment sets it; production leaves it unset once a real
# identity provider is wired up.
#
# SUNSUM_DEMO_AUTH=enabled

# Where the candidate parcel boundaries behind the site map come from.
#
#   demo    synthetic polygons in-process (the default) — three fixture parcels,
#           so the map is developable and demonstrable with no ESRI
#           subscription, no credential, and no network
#   arcgis  a live ArcGIS Feature Service layer, read server-side
#
# Same seam as SUNSUM_STORE, SUNSUM_BLOB and SUNSUM_VIABILITY. `core` owns the
# `CandidateParcelReader` interface; `src/backend/gis` speaks to ArcGIS and
# translates. An unrecognised value throws rather than falling back, because a
# typo would otherwise look like a working deployment that was quietly serving
# three fixture parcels.
#
# The ArcGIS credential is deliberately a server-only value. It is never sent to
# the browser and there is no endpoint that will relay a caller's own query to
# ESRI — GET /api/sites/candidate-parcels takes no parameters at all. A key in
# frontend code is a key published to everyone who loads the page, and our
# subscription is metered.
SUNSUM_PARCELS=demo

# The full URL of the parcel layer, including the layer index — for example
# `https://services.arcgis.com/<org>/arcgis/rest/services/<service>/FeatureServer/3`.
#
# Only read when SUNSUM_PARCELS=arcgis. Give the whole layer URL rather than a
# service URL: layer 0 is a convention, not a rule, and guessing it is how you
# end up publishing a different dataset than the one that was approved.
#
# The Atlanta/Fulton County pilot layer, verified 2026-09-21 — polygon
# geometry, Query-only capability, 12 features, maxRecordCount 2000:
# SUNSUM_ARCGIS_LAYER_URL=https://services7.arcgis.com/oytzsFXfip1CbSRi/arcgis/rest/services/SumSum_Solar_-_Candidate_Parcels_(Atlanta_and_Fulton_County)/FeatureServer/0

# OAuth 2.0 client credentials for a registered ArcGIS application. Only read
# when SUNSUM_PARCELS=arcgis, and only ever read by `src/backend/gis`.
#
# This is app authentication, which is Esri's documented model for a
# server-side process reading private content. Ask for an application scoped to
# reading the parcel layer and nothing else.
#
# A user account is not an acceptable runtime credential here, and an
# organisation administrator account least of all: it can publish, delete and
# re-share content across the whole organisation, so a leak from a running
# service would be an organisation-wide incident rather than a read-only one.
# An app credential also survives the person it was issued to changing their
# password or leaving.
#
# SUNSUM_ARCGIS_CLIENT_ID=
# SUNSUM_ARCGIS_CLIENT_SECRET=

# Interim: a named user's ArcGIS sign-in. Used only when the client credentials
# above are unset — set those and these are ignored, which is the whole
# migration.
#
# Present because the account we were given is of this kind, and an integration
# that cannot run until a different credential is issued is an integration
# nobody can review. Everything in the paragraph above still applies: this
# inherits the person's full privileges, and it stops working when they rotate
# their password or leave. Treat it as a bridge with an end date, not a
# configuration choice.
#
# SUNSUM_ARCGIS_USERNAME=
# SUNSUM_ARCGIS_PASSWORD=

# Interim only: what the user token is bound to. Not a secret — a referer is
# not a security boundary, anyone can send any value. ArcGIS simply requires a
# user token to be bound to something.
#
# Defaults to https://sunsum.local, which is fine. `requestip` binding was
# tried and does not work for us: the token mints successfully and the next
# request is refused as "Invalid token", because the token call and the query
# leave through different egress addresses behind NAT. The application
# credential needs none of this.
#
# SUNSUM_ARCGIS_REFERER=https://sunsum.local

# Where to mint the token. Defaults to the ArcGIS Online OAuth endpoint;
# override it only for ArcGIS Enterprise, which hosts its own.
#
# SUNSUM_ARCGIS_TOKEN_URL=https://www.arcgis.com/sharing/rest/oauth2/token

# How long to wait for ArcGIS before giving up, in milliseconds. Defaults to
# 10000. The map reads a cached copy, so this timeout bounds a background
# refresh rather than a user's request.
#
# SUNSUM_ARCGIS_TIMEOUT_MS=10000
